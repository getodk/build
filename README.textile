h1. odkmaker — wysiwyg web-based xforms editor for the odk subset

h2. Running

odkmaker is built on the Ruby Rack server stack, so look up how to run Rack servers, and apply that to config.ru.

For a quickstart type local debug run, make sure you have all the required gems, then type @shotgun config.ru@.

h2. Required Gems

* cloudkit
* json
* rack
* shotgun
* sinatra